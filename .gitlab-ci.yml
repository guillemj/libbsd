image: debian:buster

test:
  before_script:
    - apt-get update -qq
    - apt-get install -qq -y --no-install-recommends
              git gcc make autoconf automake libtool libmd-dev
  script:
    - ./autogen && ./configure
    - make check
